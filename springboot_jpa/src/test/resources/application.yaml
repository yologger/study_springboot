server:
  port: 6007

spring:
  application:
    name: springboot_jpa
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
#        format_sql: true
        jdbc:
          batch_size: 2

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace # show binding parameter